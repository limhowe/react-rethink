RUN THE PROJECT:
gulp dev

or:

DEBUG=rethink* gulp dev


to create records:

POST /api/users {name, email}
POST /api/conversations {title, status}
POST /api/conv-user-links {convId, userId, onlineStatus}